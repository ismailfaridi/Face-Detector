# Face-Detector

A simple Python project for detecting faces in live webcam video using OpenCV.  
This script uses OpenCV’s built-in **Haar Cascade classifier** to locate faces in live video. Haar cascades provide a lightweight and efficient method for real-time face detection.
